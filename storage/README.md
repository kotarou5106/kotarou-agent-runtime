# storage

Local persistence used by the Agent.

- `sessions/`: conversation sessions and message history.
- `memory/`: memory database and markdown stores.
- `proactive/`: proactive tick state.
- `json_store/`: small JSON persistence helpers.

Runtime user data still lives under `~/.kotarou/workspace/`.
