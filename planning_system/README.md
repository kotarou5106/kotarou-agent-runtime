# planning_system

Planning, background work, and resource-aware maintenance.

- `scheduler/`: reminders and scheduled jobs.
- `background_tasks/`: background subtask runtime.
- `drift/`: useful idle-time work when there is nothing to push.
- `resource_optimization/`: memory optimizer and cache-friendly maintenance.
